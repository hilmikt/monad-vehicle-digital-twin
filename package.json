{
  "name": "monad-vehicle-digital-twin",
  "version": "1.0.0",
  "description": "Monad Vehicle Digital Twin - NFT Car Marketplace & Tracking Dashboard",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network testnet",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.27.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  }
}
